- Deepgram Flux STT on SageMaker now reports a settings update as unsupported instead of sending it. That build accepts only `CloseStream` as a client message, so the `Configure` sent for `keyterm`, `eot_threshold`, `eager_eot_threshold`, `eot_timeout_ms`, or `language_hints` ended the session, leaving the service connected but producing no transcripts.
