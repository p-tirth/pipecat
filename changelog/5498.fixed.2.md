- Deepgram Flux STT now fails instead of hanging when Flux never confirms a connection. An endpoint that does not support a connection setting sends neither a confirmation nor an error, which left the service waiting indefinitely; the wait is now bounded and reported through `on_connection_error`.
